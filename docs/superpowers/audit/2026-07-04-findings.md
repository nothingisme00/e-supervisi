# Temuan Audit Pra-Rilis — 2026-07-04

Format entri:
## [SEVERITY] Judul singkat
- **Lokasi:** path/file.php:baris
- **Lapisan:** Keamanan | Alur Kerja | Bug | UX
- **Dampak:** apa yang bisa terjadi
- **Usulan perbaikan:** satu kalimat
- **Status:** ditemukan | disetujui | diperbaiki | dilewati

---

## Ringkasan Temuan (terurut keparahan)

Total: **1 Kritis, 8 Tinggi, 8 Sedang, 12 Rendah**. Baseline test: 150/150 lulus.

### Kritis (1)
- **K1** — Kepala sekolah dapat membuka/menyelesaikan supervisi apa pun tanpa batas status/tingkat (IDOR + transisi ilegal). `KepalaSekolah/EvaluasiController.php`

### Tinggi (8)
- **T1** — Dokumen supervisi di disk publik, bisa diakses tanpa login (`asset('storage/...')`). Guru/SupervisiController + view
- **T2** — Dependensi runtime punya advisori (symfony CVE-2025-64500 auth bypass, laravel CRLF). `composer.lock`
- **T3** — Kepala `complete()` tanpa guard status; bisa selesaikan draft/revision, reviewer tak tercatat
- **T4** — Kepala `giveFeedback`/`requestRevision` tanpa guard; draft bisa direvisi, completed dibuka ulang diam-diam
- **T5** — Admin `storeFeedback`/`requestRevision` tanpa guard status (kelas bug sama)
- **T6** — Dua reviewer (admin GET auto-mark vs kepala) berebut state machine, saling menimpa
- **T7** — Perbaikan skema-fatal (kolom salah di HEAD) masih uncommitted; discard/deploy dari HEAD menghidupkan bug SQL fatal
- *(catatan: T1–T6 semua bug perilaku; T7 adalah risiko proses)*

### Sedang (8)
- **S1** — Admin bisa menonaktifkan/menurunkan role akunnya sendiri (self-lockout)
- **S2** — Cookie sesi tak dipaksa `secure`, tak ada header keamanan, lifetime tak konsisten
- **S3** — Aturan "satu supervisi aktif" tak ditegakkan di `store()`
- **S4** — Guru bisa hapus supervisi `revision`, jejak review kepala ikut terhapus
- **S5** — `subtitle` tak ada di `$fillable` CarouselSlide, data seeder dibuang
- **S6** — `storage/framework/*/.gitignore` terhapus, fresh clone gagal boot
- **S7** — `phpunit.xml` hardcode absolute path TddGuard, rusak di CI/mesin lain
- **S8** — Form feedback/revisi kepala & admin gagal validasi diam-diam (tanpa `@error`); pesan gagal tampil bergaya sukses "Berhasil!" di proses guru; daftar kepala tak bedakan status `revision`

### Rendah (12)
- **R1** `checkDocuments` tanpa cek kepemilikan · **R2** route upload/comment/feedback tanpa rate limit · **R3** kolom mati `needs_revision` · **R4** submit ulang tinggalkan data review basi · **R5** migrasi lama diedit in-place · **R6** fallback mati `file_path` · **R7** seeder carousel salah nama file + non-idempoten · **R8** SessionTimeout silent-skip non-database · **R9** label/warna status tak konsisten antar role · **R10** pesan validasi bahasa Inggris (`APP_LOCALE=en`, tak ada `lang/`) · **R11** sisa teks Inggris di UI · **R12** flash auth (logout/info) tak tampil + notifikasi sukses ganda + toggle status tanpa konfirmasi

---

## Baseline Test Suite

**Status:** ALL PASSING

**Summary:** 150 passed (288 assertions) in 23.17s

### Test Results by Category

#### Unit Tests
- **ExampleTest** (1 test) - PASS
  - that true is true

- **CacheHelperTest** (4 tests) - PASS
  - clear dashboard cache removes all keys
  - clear user cache
  - clear supervisi cache
  - clear cache does not affect unrelated keys

- **CarouselSlideModelTest** (8 tests) - PASS
  - scope active
  - scope ordered
  - image url with full url
  - image url with images path
  - image url with storage path
  - image url returns null when no path
  - is active is cast to boolean
  - order is cast to integer

- **FeedbackModelTest** (6 tests) - PASS
  - belongs to supervisi
  - belongs to user
  - has parent relationship
  - has replies relationship
  - replies with user eager loads user
  - fillable attributes

- **SupervisiModelTest** (15 tests) - PASS
  - has status constants
  - get statuses returns all statuses
  - belongs to user
  - belongs to reviewer
  - has many dokumen evaluasi
  - has one proses pembelajaran
  - has many feedback
  - scope by status
  - scope pending
  - scope under review
  - scope completed
  - scope exclude drafts
  - tanggal supervisi is cast to date
  - needs revision is cast to boolean
  - cascade delete related records

- **UserModelTest** (11 tests) - PASS
  - user has role constants
  - is admin returns true for admin
  - is guru returns true for guru
  - is kepala sekolah returns true for kepala
  - user has supervisi relationship
  - user has feedback given relationship
  - user fillable attributes
  - is active is cast to boolean
  - must change password is cast to boolean
  - password is hidden
  - inactive factory state

#### Feature Tests
- **Admin/DashboardTest** (5 tests) - PASS
  - admin can access dashboard
  - guru cannot access admin dashboard
  - kepala cannot access admin dashboard
  - unauthenticated user redirected to login
  - dashboard shows statistics

- **Admin/SupervisiReviewTest** (9 tests) - PASS
  - admin can view supervisi list
  - supervisi list filters by status
  - admin can view supervisi detail
  - viewing submitted auto marks under review
  - admin can give feedback
  - admin can mark completed
  - feedback requires min length
  - admin can request revision
  - guru cannot access admin supervisi

- **Admin/UserManagementTest** (19 tests) - PASS
  - admin can view user list
  - user list can be searched
  - user list can be filtered by role
  - user list can be filtered by status
  - user list can be sorted
  - invalid sort column defaults to created at
  - admin can view create user form
  - admin can create guru
  - admin can create kepala sekolah
  - create user validates nik format
  - create user validates unique nik
  - guru requires tingkat and mata pelajaran
  - admin can view edit form
  - admin can update user
  - admin can reset user password
  - admin can toggle user status
  - admin can delete user
  - admin cannot delete self
  - deleting user cleans up supervisi

- **Auth/ChangePasswordTest** (4 tests) - PASS
  - change password page accessible for authenticated users
  - unauthenticated user redirected to login
  - must change password middleware redirects
  - user without must change password can access protected routes

- **Auth/LoginTest** (14 tests) - PASS
  - login page is accessible
  - login page has no cache headers
  - admin can login with valid credentials
  - guru can login with valid credentials
  - kepala sekolah can login
  - login fails with wrong password
  - login fails with wrong role
  - inactive user cannot login
  - login redirects to change password if required
  - login validation requires nik
  - login validation requires password
  - login validation requires valid role
  - authenticated user can logout
  - login sets just logged in session

- **ExampleTest** (1 test) - PASS
  - the application returns a successful response

- **Guru/ProsesTest** (7 tests) - PASS
  - guru can access proses with all docs
  - guru redirected without all docs
  - guru can save proses data
  - guru can submit supervisi
  - submit fails without complete proses
  - submit fails with incomplete proses
  - guru cannot access other guru proses

- **Guru/SupervisiFlowTest** (15 tests) - PASS
  - guru can access home
  - guru can view my supervisi
  - guru can create supervisi
  - guru with active supervisi redirected to continue
  - guru can store new supervisi
  - guru can view evaluasi page
  - guru can view own supervisi detail
  - guru cannot view other guru detail
  - guru can view other supervisi
  - guru can add comment
  - cannot comment on draft supervisi
  - guru can delete draft supervisi
  - guru cannot delete submitted supervisi
  - guru can check documents
  - admin cannot access guru routes

- **KepalaSekolah/EvaluasiTest** (13 tests) - PASS
  - kepala can access dashboard
  - kepala can view evaluasi list
  - evaluasi list can filter by status
  - evaluasi list can search
  - kepala can view supervisi detail
  - kepala can start review
  - start review fails if not submitted
  - kepala can give feedback
  - kepala can request revision via feedback
  - kepala can request revision
  - kepala can complete supervisi
  - guru cannot access kepala routes
  - admin cannot access kepala routes

- **RouteAccessTest** (11 tests) - PASS
  - root redirects to login for guests
  - root redirects admin to admin dashboard
  - root redirects guru to guru home
  - root redirects kepala to kepala dashboard
  - guru cannot access admin routes
  - guru cannot access kepala routes
  - admin cannot access guru routes
  - admin cannot access kepala routes
  - kepala cannot access admin routes
  - kepala cannot access guru routes
  - protected routes redirect to login

- **SettingsTest** (7 tests) - PASS
  - authenticated user can view settings
  - user can update profile
  - user can update password
  - password update fails with wrong current
  - password update fails with same password
  - profile update validates name
  - unauthenticated user redirected

### Conclusion

No failing tests detected. The application is ready for pre-release audit continuation.

---

# Lapisan: Keamanan (Audit Lapis 1)

## [Kritis] Kepala sekolah dapat membuka detail supervisi apa pun tanpa batas status/sekolah
- **Lokasi:** app/Http/Controllers/KepalaSekolah/EvaluasiController.php:41-54 (show); juga :56-73 (startReview), :75-126 (giveFeedback), :128-138 (complete), :140-164 (requestRevision)
- **Lapisan:** Keamanan
- **Dampak:** `show($id)` hanya memanggil `Supervisi::findOrFail($id)` tanpa filter status maupun `tingkat`. Dengan menebak/menaikkan `{id}`, kepala sekolah dapat membaca detail supervisi guru dari sekolah/tingkat lain dan supervisi berstatus draft (belum disubmit) — termasuk refleksi, proses pembelajaran, dan metadata dokumen. `downloadDocument` sengaja memaksa `user->tingkat === auth()->user()->tingkat`, membuktikan pemisahan tingkat memang diniatkan; `show()` melanggarnya (cross-user data leak). Selain itu `startReview/giveFeedback/complete/requestRevision` juga tanpa cek `tingkat`, dan `complete()` tanpa cek status sehingga bisa menyelesaikan supervisi milik sekolah lain di status apa pun.
- **Usulan perbaikan:** Tambahkan pembatasan `whereIn('status', [...submitted/under_review/revision/completed])` dan filter `tingkat` guru terhadap `auth()->user()->tingkat` pada semua method yang menerima `{id}` di EvaluasiController.
- **Status:** diperbaiki — guard `tingkat` di semua method ber-`{id}` (show/startReview/giveFeedback/complete/requestRevision) + guard status di `show` (submitted/under_review/revision/completed). Test: EvaluasiTest (kepala cannot view/start/feedback/complete/revision on other tingkat, cannot view draft).

## [Tinggi] Dokumen supervisi tersimpan di disk publik dan ditautkan langsung, dapat diakses tanpa autentikasi
- **Lokasi:** app/Http/Controllers/Guru/SupervisiController.php:109,122 (disk 'public'); resources/views/guru/supervisi/detail.blade.php:148, view.blade.php:118, evaluasi.blade.php:123,232; resources/views/kepala/evaluasi/show.blade.php:159
- **Lapisan:** Keamanan
- **Dampak:** Dokumen diunggah ke `Storage::disk('public')` (storage/app/public, disimbolik ke public/storage) dan view menautkannya via `asset('storage/' . $dokumen->path_file)`. URL seperti `/storage/supervisi/{id}/{timestamp}_{jenis}.pdf` dapat diakses siapa saja tanpa login. Route download ber-auth (`admin.supervisi.download`, `kepala.evaluasi.download`) menjadi tak berarti karena preview memakai tautan publik langsung. Path sebagian dapat ditebak (id sekuensial, jenis_dokumen dari 7 tipe tetap, hanya timestamp yang acak) — dokumen ajar bisa bocor lintas pengguna.
- **Usulan perbaikan:** Simpan dokumen di disk privat (`storage/app`, bukan `public/`) dan sajikan hanya melalui route download/preview ber-auth yang sudah memverifikasi kepemilikan/otorisasi.
- **Status:** diperbaiki — upload (PDF & gambar via ImageService disk param) pindah ke disk `local` (storage/app/private); semua delete & serve path ikut pindah. Route preview ber-auth baru: `guru.supervisi.preview` (pemilik selalu; rekan hanya non-draft) dan `kepala.evaluasi.preview` (method lama yang tak ter-route). Semua tautan `asset('storage/...')` dokumen di view diganti route preview/download ber-auth. File dev yang ada sudah dipindah ke `storage/app/private/supervisi`. **Catatan deploy:** pindahkan `storage/app/public/supervisi` → `storage/app/private/supervisi` di server. Test: DokumenAksesTest (5 test).

## [Tinggi] Dependensi runtime produksi memiliki advisori keamanan tingkat tinggi
- **Lokasi:** composer.lock — laravel/framework v12.36.1, symfony/http-foundation v7.3.5 (via `composer audit --format=plain`)
- **Lapisan:** Keamanan
- **Dampak:** `composer audit` menemukan 24 advisori pada 13 paket. Yang berdampak pada runtime produksi dan bersifat *high*: symfony/http-foundation CVE-2025-64500 (Incorrect parsing of PATH_INFO → limited authorization bypass; terpengaruh <7.3.7) dan laravel/framework CRLF injection di default email rule (terpengaruh <12.60.0; terpasang 12.36.1). Advisori *medium* runtime lain: Laravel Temporary Signed URL Path Confusion, guzzlehttp/psr7 (CRLF/host confusion), symfony/routing (URL injection), symfony/mailer & mime (header injection), league/commonmark, symfony/http-foundation SSRF; *low*: symfony/yaml (ReDoS/stack exhaustion), polyfill-intl-idn. (phpunit & psy/psysh bersifat dev-only.)
- **Usulan perbaikan:** Jalankan `composer update` untuk menaikkan laravel/framework ke >=12.61.1 dan symfony/* ke rilis patch (http-foundation >=7.3.7, dst.) lalu re-audit.
- **Status:** ditemukan

## [Sedang] Admin dapat menonaktifkan akunnya sendiri (self-lockout)
- **Lokasi:** app/Http/Controllers/Admin/UserController.php:224-233 (toggleStatus); app/Livewire/Admin/UserManagement.php:76-84 (toggleStatus)
- **Lapisan:** Keamanan
- **Dampak:** `destroy()` sudah mencegah admin menghapus dirinya sendiri (UserController.php:199), tetapi `toggleStatus()` (baik di controller maupun komponen Livewire) tidak memeriksa `$user->id === Auth::id()`. Admin bisa men-set `is_active = false` untuk akunnya sendiri; karena login menolak user tidak aktif (CustomLoginController.php:44), admin akan terkunci keluar. Terkait: `update()` juga tidak mencegah admin mengubah `role` dirinya sendiri menjadi guru/kepala sehingga kehilangan akses admin.
- **Usulan perbaikan:** Tolak `toggleStatus` (dan perubahan role) bila `$user->id === Auth::id()`.
- **Status:** ditemukan

## [Sedang] Cookie sesi tidak dipaksa `secure` + tidak ada header keamanan / lifetime tidak konsisten
- **Lokasi:** config/session.php:172 (`'secure' => env('SESSION_SECURE_COOKIE')`); .env.example:36-39; app/Http/Middleware/PreventBackHistory.php
- **Lapisan:** Keamanan
- **Dampak:** `SESSION_SECURE_COOKIE` tidak diset di .env.example dan default null → cookie sesi bisa terkirim lewat HTTP (bukan hanya HTTPS), rawan pencurian sesi bila TLS tidak dipaksa. Tidak ada header keamanan global (CSP, X-Frame-Options/clickjacking, HSTS); PreventBackHistory hanya menyetel header no-cache. Selain itu `.env.example` menyetel `SESSION_LIFETIME=120` sementara `config/session.php` default 10 dan SessionTimeout middleware timeout 10 menit — konfigurasi lifetime tidak konsisten/berpotensi membingungkan. (`http_only`=true dan `same_site`=lax sudah aman.)
- **Usulan perbaikan:** Set `SESSION_SECURE_COOKIE=true` untuk produksi, tambahkan header X-Frame-Options/CSP, dan selaraskan nilai lifetime sesi.
- **Status:** ditemukan

## [Rendah] `checkDocuments` tanpa cek kepemilikan (info disclosure minor)
- **Lokasi:** app/Http/Controllers/Guru/SupervisiController.php:182-191
- **Lapisan:** Keamanan
- **Dampak:** `checkDocuments($id)` hanya menghitung `DokumenEvaluasi::where('supervisi_id', $id)->count()` tanpa memfilter `user_id`. Guru mana pun dapat memanggil `/guru/supervisi/{id}/check-documents` untuk mengetahui jumlah dan status kelengkapan dokumen supervisi milik guru lain (kebocoran informasi terbatas; hanya angka).
- **Usulan perbaikan:** Tambahkan `Supervisi::where('id',$id)->where('user_id', auth()->id())->firstOrFail()` sebelum menghitung.
- **Status:** ditemukan

## [Rendah] Route sensitif tanpa rate limit
- **Lokasi:** routes/web.php:75 (upload), :80 (comment), :103/:128 (feedback)
- **Lapisan:** Keamanan
- **Dampak:** Hanya route login (throttle:5,1) dan change-password (throttle:10,1) yang di-rate-limit. Route upload dokumen, komentar, dan feedback tidak memiliki throttle sehingga terbuka untuk penyalahgunaan/spam/DoS ringan (misalnya unggah berulang atau banjir komentar). Catatan positif: route `logout` sudah benar berada DI LUAR grup throttle login.
- **Usulan perbaikan:** Terapkan `throttle` wajar (mis. `throttle:30,1`) pada route upload/comment/feedback.
- **Status:** ditemukan

## Observasi Aman (OK) — Lapis Keamanan
- Guru/SupervisiController: `showEvaluasi`, `uploadDocument`, `deleteDocument`, `continue`, `destroy` semua memfilter `where('user_id', auth()->id())` + status → kepemilikan aman.
- Guru/ProsesController: `show`, `save`, `submit` semua memfilter `user_id` + status draft/revision → aman.
- Guru/HomeController: `detail` (own), `mySupervisi` (own) memfilter `user_id`; `viewOther` sengaja menampilkan supervisi guru lain (fitur peer-view) dan `storeComment` memblokir komentar pada draft → sesuai desain.
- Admin/SupervisiController `downloadDocument`: path diambil dari DB (`$dokumen->path_file`), bukan input request, dan memfilter status → tidak ada path traversal.
- KepalaSekolah `downloadDocument`/`previewDocument`: path dari DB + cek `tingkat` + cek status → aman terhadap traversal (kontras dengan `show()` yang bermasalah di atas).
- Mass assignment: meski `role`/`is_active`/`must_change_password` ada di `$fillable` User, controller store/update dan SettingsController membangun array eksplisit (bukan `$request->all()`) → tidak ada eskalasi role via mass assignment.
- XSS: satu-satunya `{!! !!}` (evaluasi.blade.php:363) merender `json_encode($uploadedDocuments)` — data sistem (jenis dokumen alpha_dash), bukan teks bebas user; Blade `{{ }}` auto-escape di seluruh view lain.
- Autentikasi: `session()->regenerate()` dipanggil saat login (CustomLoginController.php:51); logout memanggil `invalidate()` + `regenerateToken()`; user non-aktif ditolak login.
- Admin `destroy` mencegah hapus akun sendiri (UserController.php:199); kolom sort di-allowlist (UserController & Livewire) → tidak ada SQL injection via `sort_by`.
- CarouselController: validasi image mimes/max ada; upload lewat ImageService; hanya di area admin.

---

# Lapisan: Alur Kerja (Audit Lapis 2)

## Peta Status

**Nilai enum resmi** (database/migrations/2025_10_31_154321_create_supervisi_table.php:14 dan 2025_11_01_083526_update_supervisi_status_enum.php:20; konstanta model app/Models/Supervisi.php:15-19 identik):

`draft` → `submitted` → `under_review` → `completed`, dengan cabang revisi `under_review` → `revision` → (guru perbaiki) → `submitted` (kembali ke antrian).

Nilai `reviewed` hanya ada di `down()` migrasi enum (legacy, tidak dipakai kode). Pemeriksaan silang: semua nilai status yang ditulis controller (`draft`, `submitted`, `under_review`, `completed`, `revision`) ada di enum, dan tidak ada nilai enum yang tak terpakai — **tidak ada temuan ketidaksesuaian enum**.

**Titik transisi di kode (working tree):**

| Transisi | Lokasi | Guard status? |
|---|---|---|
| (baru) → draft | Guru/SupervisiController.php:43-48 (store) | tidak ada cek "satu supervisi aktif" (lihat temuan Sedang) |
| draft/revision → submitted | Guru/ProsesController.php:99-126 (submit) | YA — `whereIn(['draft','revision'])` :103 |
| submitted → under_review | KepalaSekolah/EvaluasiController.php:56-73 (startReview) | YA — `!== 'submitted'` :61 |
| submitted → under_review | KepalaSekolah/EvaluasiController.php:109-115 (giveFeedback) | YA — hanya bila submitted |
| submitted → under_review | Admin/SupervisiController.php:63-68 (show, auto-mark) | YA — hanya bila submitted (tapi efek samping GET; lihat temuan) |
| apa pun → revision | KepalaSekolah/EvaluasiController.php:98-102 (giveFeedback) | TIDAK |
| apa pun → revision | KepalaSekolah/EvaluasiController.php:156-160 (requestRevision) | TIDAK |
| apa pun → revision | Admin/SupervisiController.php:119-123 (requestRevision) | TIDAK |
| apa pun → completed | KepalaSekolah/EvaluasiController.php:118-122 (giveFeedback mark_completed) | TIDAK |
| apa pun → completed | KepalaSekolah/EvaluasiController.php:128-138 (complete) | TIDAK |
| apa pun → completed | Admin/SupervisiController.php:93-98 (storeFeedback mark_completed) | TIDAK |
| draft/revision → (hapus) | Guru/SupervisiController.php:228-250 (destroy) | YA — `whereIn(['draft','revision'])` :233 |

**Alur revisi ujung-ke-ujung (tidak ada jalan buntu):** requestRevision men-set `status='revision'` + `revision_notes`; pada status `revision` guru masih bisa upload/hapus dokumen (Guru/SupervisiController.php:86,162), mengedit proses (Guru/ProsesController.php:30,77), dan submit ulang (:103) → status kembali `submitted`; antrian kepala menyertakan `submitted` (KepalaSekolah/EvaluasiController.php:21). Setiap status punya jalan keluar — state machine tidak deadlock.

## [Tinggi] KepalaSekolah `complete()` tanpa guard status — supervisi status apa pun bisa di-set `completed`
- **Lokasi:** app/Http/Controllers/KepalaSekolah/EvaluasiController.php:128-138
- **Lapisan:** Alur Kerja
- **Dampak:** `complete($id)` langsung `findOrFail` lalu `update(['status' => 'completed'])` tanpa memeriksa status saat ini. Supervisi `draft` (belum pernah disubmit), `revision` (sedang diperbaiki guru), bahkan yang sudah `completed` dapat dipaksa selesai lewat POST langsung ke `kepala/evaluasi/{id}/complete` — tombol UI memang hanya muncul saat `under_review` (show.blade.php:558) tetapi endpoint tidak menegakkannya. Selain itu `complete()` tidak men-set `reviewed_by`/`reviewed_at`, sehingga jalur `submitted → completed` langsung menghasilkan supervisi selesai tanpa reviewer tercatat. Menyelesaikan supervisi berstatus `revision` juga menelan permintaan revisi yang sedang berjalan. (Terkait temuan keamanan #1 [Kritis] — di sana soal akses lintas tingkat; di sini soal transisi ilegalnya.)
- **Usulan perbaikan:** Tolak bila `status !== 'under_review'` (atau minimal `whereIn(['submitted','under_review'])`) dan set `reviewed_by`/`reviewed_at` saat menyelesaikan.
- **Status:** diperbaiki — `complete()` menolak (403) bila status bukan `under_review`, dan men-set `reviewed_by`/`reviewed_at`. Test: complete fails if not under review; complete sets reviewed by and reviewed at.

## [Tinggi] KepalaSekolah `giveFeedback`/`requestRevision` tanpa guard status — draft bisa direvisi, completed bisa dibuka ulang diam-diam
- **Lokasi:** app/Http/Controllers/KepalaSekolah/EvaluasiController.php:75-126 (giveFeedback: revision :98-102, mark_completed :118-122), :140-164 (requestRevision :156-160)
- **Lapisan:** Alur Kerja
- **Dampak:** Keduanya `findOrFail` tanpa cek status. Konsekuensi transisi ilegal: (1) supervisi `draft` yang belum pernah disubmit bisa di-set `revision` atau langsung `completed` (via `mark_completed=1`) — `completed` dari `draft` melompati seluruh review dan `reviewed_by` tetap null; (2) supervisi `completed` bisa dikembalikan ke `revision` tanpa jejak "dibuka ulang" — di daftar guru muncul lagi banner revisi (my-supervisi.blade.php:174) padahal sudah dinyatakan selesai; (3) `revision` bisa di-set `revision` lagi sehingga `revision_notes` lama tertimpa (:101,159) — catatan revisi sebelumnya hilang dari kolom (hanya tersisa di feedback). Jalur revisi via giveFeedback pada status `submitted` juga tidak pernah men-set `reviewed_by`/`reviewed_at` (:98-102 dieksekusi sebelum :109).
- **Usulan perbaikan:** Batasi kedua endpoint ke `whereIn(['submitted','under_review'])` dan set `reviewed_by`/`reviewed_at` pada jalur revisi.
- **Status:** diperbaiki — kedua endpoint dibatasi ke `submitted/under_review/revision` (`revision` tetap diizinkan agar kepala bisa membalas diskusi saat guru merevisi; `draft` dan `completed` ditolak 403). Cabang mati `mark_completed` di `giveFeedback` kepala dihapus (UI kepala tak pernah mengirimnya; complete lewat route khusus) — menutup jalur `revision → completed` tanpa resubmit. Test: give feedback fails if draft/completed; request revision fails if completed.

## [Tinggi] Admin `storeFeedback` (mark_completed) dan `requestRevision` tanpa guard status
- **Lokasi:** app/Http/Controllers/Admin/SupervisiController.php:76-106 (storeFeedback, mark_completed :93-98), :111-135 (requestRevision :119-123)
- **Lapisan:** Alur Kerja
- **Dampak:** Kelas bug yang sama di sisi admin: `storeFeedback` dengan `mark_completed` men-set `completed` dari status apa pun (termasuk `draft` yang belum disubmit dan `revision` yang sedang dikerjakan guru), dan `requestRevision` men-set `revision` dari status apa pun (termasuk `completed` → dibuka ulang, dan `draft` → guru mendadak menerima "permintaan revisi" untuk pekerjaan yang belum pernah dikirim). `index` admin memang hanya menampilkan submitted/under_review/completed (:28), tetapi `show`/`storeFeedback`/`requestRevision` menerima `{id}` bebas sehingga status draft tetap terjangkau lewat URL langsung.
- **Usulan perbaikan:** Tambahkan guard `whereIn('status', ['submitted','under_review'])` (dan `under_review` saja untuk mark_completed) di kedua endpoint.
- **Status:** diperbaiki — `storeFeedback` & `requestRevision` dibatasi ke `submitted/under_review/revision` (draft/completed ditolak 403, konsisten dengan guard sisi kepala); `mark_completed` tambahan hanya boleh dari `submitted/under_review` (menutup `revision → completed` tanpa resubmit). Test: admin store feedback fails if draft or completed; admin mark completed fails if revision; admin request revision fails if draft or completed.

## [Tinggi] Dua jalur reviewer (admin vs kepala sekolah) berebut state machine yang sama — saling menimpa tanpa koordinasi
- **Lokasi:** app/Http/Controllers/Admin/SupervisiController.php:63-68 (show auto-mark); app/Http/Controllers/KepalaSekolah/EvaluasiController.php:56-73 (startReview), :128-138 (complete)
- **Lapisan:** Alur Kerja
- **Dampak:** Admin `show()` (request GET) otomatis mengubah `submitted → under_review` dan men-set `reviewed_by = admin` (tanpa `reviewed_at`, :63-68) hanya karena membuka halaman detail. Akibatnya: (1) `startReview` kepala sekolah selalu ditolak dengan pesan menyesatkan "Supervisi sudah dalam proses review" (:61-62) padahal tidak ada yang mereview; (2) atribusi reviewer direbut admin yang sekadar melihat; (3) selanjutnya kepala tetap bisa `complete`/`requestRevision` dan admin juga — status mengikuti siapa pun yang menulis terakhir (last-write-wins), mis. kepala meminta `revision` lalu admin `mark_completed` menimpanya menjadi `completed` tanpa peringatan, atau sebaliknya. Tidak ada mekanisme klaim/kunci reviewer.
- **Usulan perbaikan:** Hapus auto-mark pada GET admin (jadikan aksi eksplisit) dan tolak perubahan status bila `reviewed_by` sudah diisi reviewer lain (atau tetapkan satu peran sebagai pemilik alur review).
- **Status:** diperbaiki — auto-mark di GET admin `show()` dihapus; promosi `submitted → under_review` (+`reviewed_by`/`reviewed_at`) kini terjadi eksplisit saat admin mengirim feedback. Klaim reviewer via `Supervisi::lockedByOther()`: selama `under_review`, perubahan status (complete/mark_completed/revisi) oleh reviewer lain ditolak 403; klaim lepas saat status kembali `submitted` (resubmit). Feedback/balasan diskusi biasa tetap boleh lintas reviewer. Test: viewing submitted does not change status; admin feedback promotes submitted to under review; admin/kepala cannot change status of review claimed by other.

## [Sedang] Aturan "satu supervisi aktif" tidak ditegakkan di `store()` — hanya di form GET
- **Lokasi:** app/Http/Controllers/Guru/SupervisiController.php:40-52 (store); pembanding :25-38 (create)
- **Lapisan:** Alur Kerja
- **Dampak:** `create()` mengalihkan guru yang masih punya supervisi `draft`/`revision` ke halaman lanjutkan (:28-34), tetapi `store()` langsung `Supervisi::create` tanpa cek yang sama. POST langsung ke `guru/supervisi/store` (atau double-click/refresh form) menghasilkan banyak draft aktif sekaligus, melanggar asumsi satu-supervisi-aktif yang dipakai alur `create → continue`.
- **Usulan perbaikan:** Ulangi cek supervisi aktif di awal `store()` dan redirect ke `continue` bila ada.
- **Status:** ditemukan

## [Sedang] Guru dapat menghapus supervisi berstatus `revision` — riwayat review kepala sekolah ikut lenyap
- **Lokasi:** app/Http/Controllers/Guru/SupervisiController.php:228-250 (destroy, guard :233); app/Models/Supervisi.php:67-82 (cascade delete feedback)
- **Lapisan:** Alur Kerja
- **Dampak:** Guard `destroy` benar menolak `submitted`/`under_review`/`completed`, tetapi mengizinkan `revision`. Supervisi berstatus `revision` sudah melewati review dan memuat feedback/permintaan revisi kepala sekolah; `deleting` hook menghapus seluruh feedback (Supervisi.php:78). Guru yang tidak mau merevisi cukup menghapus supervisinya: permintaan revisi hilang diam-diam dari antrian kepala (index menyertakan `revision`, EvaluasiController.php:21) tanpa notifikasi, dan jejak audit review terhapus.
- **Usulan perbaikan:** Batasi `destroy` hanya untuk `draft`, atau minimal beri penanda/notifikasi ke reviewer saat supervisi ber-feedback dihapus.
- **Status:** ditemukan

## [Rendah] Kolom `needs_revision` mati — tidak pernah ditulis oleh kode aplikasi
- **Lokasi:** database/migrations/2025_11_04_084435_add_revision_fields_to_supervisi_table.php:15; app/Models/Supervisi.php:28,35; satu-satunya penulis: database/factories/SupervisiFactory.php:61
- **Lapisan:** Alur Kerja
- **Dampak:** Kolom boolean `needs_revision` ada di skema, fillable, dan cast, tetapi tidak ada satu pun controller yang men-set-nya (semua jalur revisi hanya menulis `status` + `revision_notes`). View pun menandai revisi dari `status == 'revision'` (my-supervisi.blade.php:174). Kolom selalu `false` di data nyata; siapa pun yang kelak mengandalkannya akan mendapat data salah.
- **Usulan perbaikan:** Hapus kolom (beserta cast/fillable) atau set konsisten di setiap transisi masuk/keluar `revision`.
- **Status:** ditemukan

## [Rendah] Submit ulang tidak membersihkan sisa siklus review sebelumnya
- **Lokasi:** app/Http/Controllers/Guru/ProsesController.php:122-126 (submit)
- **Lapisan:** Alur Kerja
- **Dampak:** Saat guru submit ulang setelah revisi, hanya `status` dan `tanggal_supervisi` yang diperbarui; `revision_notes`, `reviewed_by`, `reviewed_at` dari siklus sebelumnya tetap tersimpan. Dampak tampilan kecil karena banner revisi guru di-gate `status == 'revision'` (my-supervisi.blade.php:174), tetapi data basi ini menyesatkan query/laporan apa pun yang membaca `reviewed_by`/`revision_notes` tanpa melihat status, dan `tanggal_supervisi` awal tertimpa `now()` setiap submit ulang (tanggal submit pertama hilang).
- **Usulan perbaikan:** Saat submit ulang, kosongkan `revision_notes` (dan pertimbangkan mempertahankan `tanggal_supervisi` pertama).
- **Status:** ditemukan

## Observasi Aman (OK) — Lapis Alur Kerja
- Enum vs kode: semua nilai status yang ditulis controller ada di enum resmi; tidak ada nilai liar (nilai legacy `reviewed` hanya di `down()` migrasi).
- Guru/ProsesController `submit` (:103): guard `whereIn(['draft','revision'])` → tidak bisa submit dua kali (submitted/under_review/completed ditolak 404) dan submit divalidasi kelengkapan proses (:109-120).
- Guru/ProsesController `show`/`save` dan Guru/SupervisiController `showEvaluasi`/`uploadDocument`/`deleteDocument`: semua di-guard `whereIn(['draft','revision'])` → dokumen/proses tidak bisa diubah setelah submit. Alur revisi tetap bisa edit sesuai desain.
- Guru/SupervisiController `destroy` menolak submitted/under_review/completed (:233) — sesuai harapan (kecuali kasus `revision` di atas).
- KepalaSekolah `startReview` satu-satunya endpoint kepala dengan guard status benar (:61).
- Alur revisi ujung-ke-ujung tidak memiliki jalan buntu: `revision → submitted → under_review/completed` berfungsi; antrian kepala menyertakan submitted dan revision (EvaluasiController.php:21).

---

# Lapisan: Bug (Audit Lapis 3)

## Verifikasi Baseline Test (Step 3)

Test suite dijalankan ulang pada working tree (dengan seluruh perubahan uncommitted aktif): **150 passed (288 assertions), 6.88s** — identik dengan baseline Task 1. Tidak ada test gagal untuk dianalisis; tidak ada temuan dari lapis ini yang berasal dari kegagalan test.

## Catatan Konteks Working Tree

Working tree memuat banyak perubahan `M` yang belum di-commit. Hasil review diff (`git diff app/ database/`): **mayoritas perubahan tersebut adalah PERBAIKAN bug fatal yang ada di HEAD**, bukan bug baru. Verifikasi silang migrasi vs kode membuktikan kolom yang dipakai HEAD tidak pernah ada di skema (lihat temuan Tinggi pertama di bawah). Konsistensi migrasi vs model vs pemakaian untuk `Supervisi`, `DokumenEvaluasi`, `Feedback`, `ProsesPembelajaran` diverifikasi kolom-per-kolom — hasil di temuan dan Observasi OK.

## [Tinggi] Perbaikan bug skema-fatal alur review masih berstatus uncommitted — HEAD (commit terakhir) memakai kolom yang tidak pernah ada
- **Lokasi:** git diff pada app/Http/Controllers/Admin/SupervisiController.php:66,86-98,119-131 dan app/Http/Controllers/KepalaSekolah/EvaluasiController.php:98-102,156-160; pembanding skema: database/migrations/2025_10_31_154412_create_feedback_table.php (kolom `user_id`,`komentar` — tidak ada `admin_id`/`feedback`) dan 2025_10_31_154321_create_supervisi_table.php (kolom `reviewed_by` — tidak ada `reviewer_id`/`revision_count`)
- **Lapisan:** Bug
- **Dampak:** Versi HEAD menulis ke kolom yang tidak ada di migrasi mana pun: `reviewer_id` (admin show/storeFeedback/requestRevision), `revision_count` (admin & kepala requestRevision, kepala giveFeedback), `admin_id`+`feedback` (Feedback::create admin). Di HEAD, membuka detail supervisi submitted sebagai admin atau meminta revisi sebagai kepala/admin akan melempar SQL error "Unknown column" — alur review admin mati total. Working tree sudah memperbaikinya (rename ke `reviewed_by`/`user_id`/`komentar`, hapus `revision_count`, tambah `is_revision_request`), form view sudah cocok (admin/supervisi/detail.blade.php:248 `name="komentar"`, :263 `name="mark_completed"`, :322 `name="revision_notes"`), dan 150 test lulus — tetapi perbaikan ini belum di-commit. Risiko: checkout/deploy dari HEAD, `git stash`/discard, atau commit parsial akan menghidupkan kembali bug fatal ini.
- **Usulan perbaikan:** Segera commit seluruh perubahan perbaikan skema di kedua controller (beserta view pasangannya) sebagai satu commit utuh.
- **Status:** ditemukan

## [Sedang] `subtitle` tidak ada di `$fillable` CarouselSlide — data seeder dibuang diam-diam, tampilan carousel kehilangan baris subjudul
- **Lokasi:** app/Models/CarouselSlide.php:12-18 ($fillable tanpa `subtitle`); database/migrations/2025_12_26_080000_create_carousel_slides_table.php:16 (kolom `subtitle` ada); database/seeders/CarouselSlideSeeder.php:32,40,48 (mengirim `subtitle` via `CarouselSlide::create`); resources/views/auth/change-password.blade.php:295 (merender `$slide->subtitle`)
- **Lapisan:** Bug
- **Dampak:** `CarouselSlide::create($slide)` membuang atribut non-fillable tanpa error, sehingga `subtitle` ("Pendidikan Digital", "Real-time", "Guru & Kepala Sekolah") tidak pernah tersimpan. Halaman ganti-password merender `$slide->subtitle` yang selalu kosong — teks gradien subjudul hilang. Admin CarouselController (store :42, update :93) juga tidak pernah menangani `subtitle`, jadi tidak ada jalur lain untuk mengisinya; kolom efektif mati.
- **Usulan perbaikan:** Tambahkan `'subtitle'` ke `$fillable` CarouselSlide (dan idealnya ke form/validasi CarouselController).
- **Status:** ditemukan

## [Sedang] File scaffolding `storage/framework/*/.gitignore` dihapus dari repo — fresh clone gagal boot
- **Lokasi:** git status: `D storage/framework/cache/data/.gitignore`, `D storage/framework/sessions/.gitignore`, `D storage/framework/views/.gitignore`
- **Lapisan:** Bug
- **Dampak:** File `.gitignore` ini adalah placeholder standar Laravel agar direktori `storage/framework/{cache/data,sessions,views}` ikut ter-track git. Bila penghapusan ini di-commit, clone/deploy baru tidak memiliki direktori tersebut → Laravel gagal ("Please provide a valid cache path" / gagal compile view) sampai direktori dibuat manual. Lingkungan dev saat ini tidak terasa karena direktorinya sudah ada di disk.
- **Usulan perbaikan:** Restore ketiga file dengan `git checkout -- storage/framework/` (jangan ikutkan penghapusannya dalam commit).
- **Status:** ditemukan

## [Sedang] phpunit.xml memuat ekstensi TddGuard dengan absolute path mesin lokal — test suite berpotensi rusak di mesin lain/CI
- **Lokasi:** phpunit.xml:7-9 (uncommitted): `<bootstrap class="TddGuard\PHPUnit\TddGuardExtension"><parameter name="projectRoot" value="D:\Dokumen\Abang Punya\Project\e-supervisi"/></bootstrap>`; composer.json:24 (`tdd-guard/phpunit` di require-dev)
- **Lapisan:** Bug
- **Dampak:** `projectRoot` di-hardcode ke path Windows mesin developer. Di mesin lain/CI (terlebih Linux) path itu tidak ada sehingga ekstensi menulis data guard ke lokasi salah atau gagal saat bootstrap PHPUnit — seluruh suite bisa gagal jalan. Ini artefak tooling dev personal yang tercampur ke konfigurasi bersama. (Perubahan lain di phpunit.xml — paksa sqlite `:memory:` + `force="true"` — justru baik dan membuat test tidak bergantung .env lokal.)
- **Usulan perbaikan:** Keluarkan blok `<extensions>` TddGuard dari phpunit.xml yang di-commit (mis. pakai phpunit.xml lokal/override), pertahankan sisanya.
- **Status:** ditemukan

## [Rendah] Tujuh file migrasi lama diedit in-place — anti-pattern deploy, meski skema akhir terverifikasi konvergen
- **Lokasi:** git diff database/migrations/: 2025_10_31_154321_create_supervisi_table.php:14-15 (enum diganti dari `['draft','submitted','reviewed','completed']` ke 5 nilai final + `tanggal_supervisi` jadi nullable di create); 2025_12_26_080000_create_carousel_slides_table.php:16 (`title` jadi nullable di create); skip-SQLite ditambahkan ke 2025_01_02 (nik), 2025_11_01 (enum), 2025_11_06 (indexes), 2025_11_07 (tanggal nullable), 2026_01_07 (title nullable)
- **Lapisan:** Bug
- **Dampak:** Migrasi yang sudah pernah dijalankan di lingkungan lain tidak akan dieksekusi ulang (Laravel melacak per nama file), sehingga mengedit isinya menciptakan dua "sejarah skema" berbeda. Diverifikasi: dampak nyata NIHIL karena migrasi lanjutan tetap mengkonvergenkan skema di MySQL (2025_11_01 menyamakan enum, 2025_11_07 menyamakan nullable, 2026_01_07 menyamakan title) dan skip-SQLite hanya berpengaruh di DB test; guard skip juga benar (import `DB` ada di 2025_01_02:6). Tetap dicatat karena praktik ini rawan menimbulkan drift diam-diam pada edit berikutnya, dan test SQLite kini bergantung pada isi create-migration yang diedit.
- **Usulan perbaikan:** Ke depan gunakan migrasi baru untuk perubahan skema; anggap file migrasi yang sudah dirilis bersifat immutable.
- **Status:** ditemukan

## [Rendah] Fallback mati `$dokumen->file_path` — kolom yang tidak pernah ada
- **Lokasi:** app/Http/Controllers/KepalaSekolah/EvaluasiController.php:183,217 (`$dokumen->path_file ?? $dokumen->file_path ?? null`); resources/views/kepala/evaluasi/show.blade.php:155
- **Lapisan:** Bug
- **Dampak:** Tabel `dokumen_evaluasi` hanya punya `path_file` (migrasi 2025_10_31_154338); atribut `file_path` selalu null sehingga cabang fallback tidak pernah berguna — kode mati yang menyesatkan pembaca seolah ada dua skema. Tidak ada dampak runtime (akses atribut tak dikenal di Eloquent mengembalikan null, bukan error).
- **Usulan perbaikan:** Hapus fallback `?? $dokumen->file_path` di ketiga lokasi.
- **Status:** ditemukan

## [Rendah] CarouselSlideSeeder: nama file sampel tidak cocok dengan file yang ada + tidak idempoten
- **Lokasi:** database/seeders/CarouselSlideSeeder.php:22-26 (mencari `education-1..3.jpg`) vs isi nyata public/images/carousel/samples/ (`brooke-cagle-*.jpg`, `edwin-andrade-*.jpg`, `kenny-eliason-*.jpg`, `kimberly-farmer-*.jpg`); :56-90 (loop create tanpa cek duplikat)
- **Lapisan:** Bug
- **Dampak:** Seeder mencari `education-N.jpg` yang tidak ada, padahal empat gambar sampel tersedia dengan nama berbeda di direktori yang sama — semua slide ter-seed tanpa gambar (hanya warning di console) meski asetnya ada. Selain itu setiap `db:seed` ulang menambah 3 slide duplikat (tidak pakai `updateOrCreate`/truncate), menggandakan isi carousel login.
- **Usulan perbaikan:** Samakan daftar nama file dengan aset nyata (atau baca isi direktori) dan gunakan `updateOrCreate` agar idempoten.
- **Status:** ditemukan

## [Rendah] SessionTimeout dinonaktifkan diam-diam bila `SESSION_DRIVER` bukan `database`
- **Lokasi:** app/Http/Middleware/SessionTimeout.php:29-31 (uncommitted); config/session.php:21 (default `database`)
- **Lapisan:** Bug
- **Dampak:** Guard baru `if (config('session.driver') !== 'database') return $next($request);` benar untuk testing (driver array tidak punya tabel sessions), tetapi juga berarti deploy produksi yang mengganti `SESSION_DRIVER` ke `file`/`redis` akan kehilangan enforcement timeout-inaktivitas 10 menit tanpa peringatan apa pun. Default `.env.example` dan config = `database`, jadi tidak ada dampak pada konfigurasi standar — risikonya konfigurasi menyimpang yang tak terdeteksi. (Terkait temuan Keamanan [Sedang] soal konsistensi lifetime sesi.)
- **Usulan perbaikan:** Dokumentasikan ketergantungan ini atau fallback ke pengecekan `last_activity` berbasis session store bawaan saat driver bukan database.
- **Status:** ditemukan

## Observasi Aman (OK) — Lapis Bug
- Konsistensi migrasi vs model vs pemakaian terverifikasi: `Supervisi` ($fillable = kolom migrasi: user_id, status, tanggal_supervisi, catatan, reviewed_by, reviewed_at, needs_revision, revision_notes), `Feedback` (supervisi_id, user_id, parent_id, komentar, rating, is_revision_request — semua ada di migrasi), `DokumenEvaluasi`, `ProsesPembelajaran` (link_video, link_meeting, refleksi_1..5) — tidak ada kolom dipakai kode yang absen dari skema pada working tree (satu-satunya penyimpangan: `subtitle` di temuan Sedang, arah sebaliknya).
- Perbaikan uncommitted lain yang diverifikasi benar dan konsisten: view admin/detail & kepala/show `file_path→path_file`, `video_link→link_video`, `meeting_link→link_meeting`; guru/view.blade `$dokumen->path→path_file` dan `$supervisi->mata_pelajaran→$supervisi->user->mata_pelajaran` (kolom mata_pelajaran memang milik users, bukan supervisi); duplikat `</body>` login.blade dihapus; header `X-CSRF-TOKEN` ditambahkan pada fetch auto-save proses.blade:699 (sebelumnya auto-save selalu gagal 419).
- Penghapusan `link_meeting` dari cek `continue()` (Guru/SupervisiController.php:213-220) konsisten: `submit()` (ProsesController.php:109-115) dan validasi `save()` (:67 `nullable|url`) sama-sama memperlakukan link_meeting opsional; UI proses.blade:729-730 juga men-skip field ini.
- `CarouselSlide::getImageUrlAttribute` (3 cabang: URL penuh / `images/` publik / storage) konsisten dengan penghasil path: ImageService menghasilkan `carousel/...`, `avatars/...` (cabang storage) dan seeder menghasilkan `carousel/...`; dicakup 4 unit test image url yang lulus.
- `CacheHelper`: semua kunci `cache()->remember` yang hidup (stats.users_all, stats.supervisi_all, admin.guru_list, admin.recent_supervisi, kepala.stats.all, kepala.recent_supervisi) tercakup daftar clear; kunci "legacy" ekstra hanya redundan, tidak berbahaya. Catatan: kunci `kepala.*` bersifat global — saat ini konsisten karena dashboard/index kepala memang tanpa filter tingkat (itu masalah otorisasi yang sudah dicatat di temuan Keamanan [Kritis]); bila filter tingkat ditambahkan, kunci cache HARUS ikut di-key per tingkat.
- `ImageService`: fallback GD→simpleUpload di semua jalur, try/catch menyeluruh, tidak ada logic error yang ditemukan.
- Eager load `feedback` ditambahkan di `mySupervisi` (Guru/HomeController.php:45) — menghilangkan lazy-load N+1 untuk `$item->feedback->count()` di my-supervisi.blade:123; benar.
- tests/: `ExampleTest` diperbarui mengikuti perilaku redirect root (bukan test usang); `tests/Unit/SupervisiModelTest.php` yang dihapus adalah file kosong 0 byte (duplikat dari tests/Unit/Models/SupervisiModelTest.php yang aktif).
- `vendor.zip` (file tracked) dihapus dari repo — perbaikan hygiene, bukan bug.

---

# Lapisan: UX (Audit Lapis 4)

## Layar yang Ditelusuri

- **Guru:** `guru/home` → `guru/supervisi/create` → `evaluasi` (upload 7 dokumen) → `proses` (form + submit) → `detail`; juga `my-supervisi` dan `view` (peer-view).
- **Kepala Sekolah:** `kepala/dashboard`, `kepala/evaluasi/index`, `kepala/evaluasi/show` (feedback, minta revisi, tandai selesai).
- **Admin:** `admin/dashboard`, `admin/users/*` (Livewire index + create/edit), `admin/supervisi/index+detail`, `admin/carousel/index`.
- **Auth:** `auth/login`, `auth/change-password`; layout utama `layouts/modern.blade.php`.

Mekanisme notifikasi global: layout merender flash `session('success'/'error'/'warning'/'info')` sebagai toast modal (modern.blade.php:1477-1488) — bekerja untuk semua halaman ber-layout.

## [Sedang] Form feedback & minta revisi kepala sekolah gagal validasi secara diam-diam — tidak ada `@error` sama sekali di halaman
- **Lokasi:** resources/views/kepala/evaluasi/show.blade.php:519-571 (form feedback, textarea `komentar` :526-532), :587-638 (modal revisi, `revision_notes` :611-618) — tidak ada satu pun `@error`/`$errors` di file; aturan server: app/Http/Controllers/KepalaSekolah/EvaluasiController.php:77-80 (`komentar` min:10), :142-144 (`revision_notes` min:10)
- **Lapisan:** UX
- **Dampak:** Textarea hanya ber-atribut `required` tanpa `minlength`, jadi komentar/catatan revisi < 10 karakter lolos browser tetapi ditolak server → redirect back. Halaman tidak merender `$errors` dan toast layout hanya membaca flash `session('error')` (bukan error bag), sehingga TIDAK ADA pesan apa pun yang muncul; modal revisi bahkan tertutup lagi setelah reload. Kepala sekolah mengira feedback/permintaan revisi terkirim padahal hilang. Form balasan (reply, :382-387, :449-454) bernasib sama.
- **Usulan perbaikan:** Tambahkan blok `@error('komentar')`/`@error('revision_notes')` (dan buka kembali modal saat `$errors->has('revision_notes')`), plus atribut `minlength="10"` di textarea.
- **Status:** ditemukan

## [Sedang] Modal "Minta Revisi" admin: error validasi `revision_notes` tidak pernah terlihat
- **Lokasi:** resources/views/admin/supervisi/detail.blade.php:298-341 (modal revisi; `revision_notes` :318-323, tanpa `@error`); aturan server: app/Http/Controllers/Admin/SupervisiController.php:113-115 (min:10)
- **Lapisan:** UX
- **Dampak:** Kelas masalah yang sama di sisi admin: `@error('komentar')` untuk form feedback ada (:255-257, bagus), tetapi `revision_notes` tidak — catatan revisi < 10 karakter → redirect back, modal tertutup, tanpa pesan; admin mengira permintaan revisi terkirim ke guru padahal tidak.
- **Usulan perbaikan:** Tambahkan `@error('revision_notes')` di dalam modal + auto-buka modal bila error bag berisi `revision_notes`; tambahkan `minlength="10"`.
- **Status:** ditemukan

## [Sedang] Halaman proses guru: pesan GAGAL ditampilkan dengan gaya SUKSES ("Berhasil!" + ikon hijau)
- **Lokasi:** resources/views/guru/supervisi/proses.blade.php:438-458 (definisi lokal `showToast(message, isError)`), pemanggil error :535, :671, :678, :685; penyebab: layouts/modern.blade.php:611 (`@yield('content')` dirender SEBELUM skrip layout) dan :1348 (`function showToast(message, type)` layout mendeklarasi ulang nama global yang sama, :1491 `window.showToast = showToast`)
- **Lapisan:** UX
- **Dampak:** Deklarasi `showToast` layout dieksekusi setelah skrip halaman, menimpa versi lokal. Panggilan halaman `showToast('Gagal submit …', true)` masuk ke fungsi layout dengan `type === true` → semua lookup (`titles[true]`, `iconBgs[true]`) jatuh ke fallback `success`: judul "Berhasil!", ikon centang hijau, progress bar emerald — dengan isi teks pesan kegagalan. Skenario nyata: guru mengisi `link_video` bukan URL valid (klien hanya cek non-kosong; kirim via fetch sehingga validasi `type=url` browser tak berjalan) → server 422 → toast tampil bergaya sukses berisi pesan error (berbahasa Inggris pula, lihat temuan APP_LOCALE). Guru bisa mengira submit berhasil.
- **Usulan perbaikan:** Ganti pemanggil di proses.blade menjadi `showToast(msg, 'error')` (atau ganti nama fungsi lokal agar tidak bentrok dengan layout).
- **Status:** ditemukan

## [Sedang] Daftar evaluasi kepala: supervisi status `revision` tampil TANPA badge dan dengan aksen hijau seolah selesai
- **Lokasi:** resources/views/kepala/evaluasi/index.blade.php:121 (aksen: `@if submitted … @elseif under_review … @else` hijau emerald), :127-133 (badge hanya untuk submitted/under_review/completed), :44-73 (filter status tanpa opsi `revision`); antrian menyertakan `revision`: app/Http/Controllers/KepalaSekolah/EvaluasiController.php:21
- **Lapisan:** UX
- **Dampak:** Controller sengaja memasukkan status `revision` ke daftar ("so kepala can see supervisi that need revision"), tetapi kartu untuk item `revision` tidak menampilkan badge status apa pun dan garis aksen kirinya jatuh ke cabang `@else` = gradasi hijau — visual yang sama dengan "selesai". Kepala sekolah tidak bisa membedakan item yang sedang menunggu revisi guru, dan tidak ada opsi filter "Perlu Revisi" di dropdown status.
- **Usulan perbaikan:** Tambahkan cabang badge + warna khusus (mis. rose "PERLU REVISI") dan opsi filter `revision` di dropdown.
- **Status:** ditemukan

## [Rendah] Label dan warna status supervisi tidak konsisten antar layar dan antar role
- **Lokasi:** guru/home.blade.php:156-188 (submitted=**amber** "Disubmit", under_review=indigo "Direview", completed="Selesai"); guru/my-supervisi.blade.php:76-112 (submitted=**biru** "Disubmit", under_review=**amber** "Ditinjau", completed="Selesai"); guru/supervisi/detail.blade.php:68-97 & view.blade.php:61-80, admin/supervisi/detail.blade.php:26-41, kepala/evaluasi/show.blade.php:58-87 (submitted="Menunggu Peninjauan", under_review="Sedang Ditinjau", completed=**"Telah Ditinjau"**); kepala/evaluasi/index.blade.php:128-132 ("REVIEW"/"DITINJAU"/"SELESAI")
- **Lapisan:** UX
- **Dampak:** Status yang sama diberi nama berbeda-beda: `under_review` = "Direview" / "Ditinjau" / "Sedang Ditinjau" / "DITINJAU"; `completed` = "Selesai" / "Telah Ditinjau" / "SELESAI" — "Telah Ditinjau" di halaman detail mudah dikira status antara, padahal final. Warna juga bertukar antara dua layar guru: amber berarti *submitted* di home tetapi berarti *under_review* di my-supervisi (submitted jadi biru) — pengguna yang mengandalkan warna akan salah baca status. Tips di home (:70) bahkan menyebut set label lain lagi ("Draft, Disubmit, Direview, Selesai").
- **Usulan perbaikan:** Definisikan satu komponen/partial badge status (label + warna baku per status) dan pakai di semua view ketiga role.
- **Status:** ditemukan

## [Rendah] Pesan validasi framework tampil dalam bahasa Inggris — tidak ada folder `lang/` dan `APP_LOCALE=en`
- **Lokasi:** config/app.php:81 (`'locale' => env('APP_LOCALE', 'en')`); .env.example:7 (`APP_LOCALE=en`); tidak ada direktori `lang/` di root proyek; contoh rule tanpa pesan kustom: Auth/CustomLoginController.php:27-31, Admin/SupervisiController.php:78-81 & :113-115, KepalaSekolah/EvaluasiController.php:77-80 & :142-144, Guru/HomeController.php:76-79, Admin/CarouselController.php:35-39 & :86-90
- **Lapisan:** UX
- **Dampak:** Semua pesan error validasi bawaan dirender dari lang default vendor (Inggris): kolom NIK kosong di login menghasilkan "The nik field is required.", komentar terlalu pendek → "The komentar field must be at least 10 characters." — bercampur dengan UI yang sepenuhnya Indonesia. Beberapa form memang punya pesan kustom Indonesia (SettingsController, ChangePasswordController, UserController), sehingga nadanya tambal sulam antar layar.
- **Usulan perbaikan:** Tambahkan `lang/id/validation.php` (publikasikan & terjemahkan) dan set `APP_LOCALE=id`, atau lengkapi pesan kustom pada setiap `validate()`.
- **Status:** ditemukan

## [Rendah] Sisa teks Inggris di UI berbahasa Indonesia
- **Lokasi:** admin/dashboard.blade.php:164 ("Submitted"), :166 ("Review"), :217 ("Completed"); layouts/modern.blade.php:2300 ("Completed & In Progress"), :2315 ("Completed"), :2329 ("Revision"), :2801 ("Status: Submitted"), :2810 ("Status: Revision Requested"); guru/home.blade.php:56 ("Quick Navigation")
- **Lapisan:** UX
- **Dampak:** Badge status di kartu dashboard admin dan konten modal panduan memakai istilah Inggris, sementara layar lain memakai label Indonesia untuk status yang sama — menambah kebingungan label status (lihat temuan konsistensi label) dan menurunkan kesan konsistensi bahasa. (Istilah serapan umum seperti "Upload", "Preview", "Progress", "Feedback" dipakai konsisten dan tidak dihitung.)
- **Usulan perbaikan:** Ganti ke label Indonesia baku yang sama dengan layar lain ("Disubmit", "Ditinjau", "Selesai", "Revisi").
- **Status:** ditemukan

## [Rendah] Flash message di alur auth tidak pernah tampil (logout & info wajib ganti password)
- **Lokasi:** Auth/CustomLoginController.php:85-87 (logout → `with('success', 'Anda telah berhasil logout…')`) vs resources/views/auth/login.blade.php:477-482 (hanya merender `$errors`, tidak ada blok `session('success')`); CustomLoginController.php:58-59 (`with('info', 'Anda harus mengganti password default…')`) vs resources/views/auth/change-password.blade.php:386-395 (hanya merender `$errors`, tidak ada blok `session('info')`)
- **Lapisan:** UX
- **Dampak:** Kedua halaman auth berdiri sendiri (tidak memakai layouts/modern yang punya toast), sehingga pesan perpisahan logout dan — lebih penting — penjelasan MENGAPA pengguna baru dipaksa ke halaman ganti password tidak pernah terlihat. Pengguna baru tiba di form ganti password tanpa konteks.
- **Usulan perbaikan:** Render blok `session('success')`/`session('info')` di login.blade dan change-password.blade.
- **Status:** ditemukan

## [Rendah] Notifikasi sukses ganda/tidak konsisten: toast global + banner inline + `alert()` native
- **Lokasi:** layouts/modern.blade.php:1477-1488 (toast global untuk semua flash); duplikat inline: guru/supervisi/detail.blade.php:21-41, guru/supervisi/view.blade.php:259-267, kepala/evaluasi/show.blade.php:19-25 DAN :308-317 (dua banner di halaman yang sama); admin/supervisi/detail.blade.php:349-352 (`alert("{{ session('success') }}")` native)
- **Lapisan:** UX
- **Dampak:** Satu aksi sukses memunculkan pesan yang sama 2× (toast modal + banner inline; di kepala/show malah 3× karena banner dirender di dua tempat), dan di detail admin dipakai `alert()` browser native yang tampil bersamaan dengan toast bergaya aplikasi — pengalaman tidak konsisten antar layar meski tidak menyesatkan.
- **Usulan perbaikan:** Pilih satu kanal (toast global) dan hapus banner inline duplikat serta `alert()` di detail admin.
- **Status:** ditemukan

## [Rendah] Kelola Pengguna: toggle status akun satu-klik tanpa konfirmasi; pesan reset password hardcode
- **Lokasi:** resources/views/livewire/admin/user-management.blade.php:308-313 & :469-473 (`wire:click="toggleStatus"` langsung pada badge status); app/Livewire/Admin/UserManagement.php:76-84; resources/views/admin/users/index.blade.php:45 (teks konfirmasi "Password akan direset ke: pass123456") vs app/Http/Controllers/Admin/UserController.php:93 (`env('DEFAULT_USER_PASSWORD', 'pass123456')`)
- **Lapisan:** UX
- **Dampak:** Aksi destruktif lain (hapus user, reset password) memakai modal konfirmasi, tetapi menonaktifkan akun cukup satu klik pada badge — mudah tersentuh tak sengaja di tabel mobile, dan langsung mengunci user dari login (termasuk akun admin sendiri, lihat temuan Keamanan [Sedang]). Selain itu teks konfirmasi reset password menuliskan "pass123456" secara hardcode; bila produksi men-set `DEFAULT_USER_PASSWORD` berbeda, admin akan menyampaikan password yang salah ke pengguna.
- **Usulan perbaikan:** Bungkus `toggleStatus` dengan `wire:confirm`/modal konfirmasi, dan ambil password default dari config untuk teks konfirmasi.
- **Status:** ditemukan

## Observasi Aman (OK) — Lapis UX
- **Empty state:** ada dan informatif di semua daftar — guru/home (:400-422, plus modal onboarding + CTA "Buat Supervisi Baru"), my-supervisi, kepala/evaluasi/index (:187-197, dengan tombol reset filter), admin dashboard (3 kartu @forelse), admin/supervisi/index (:126), admin/carousel (:91-107), Livewire user-management (:345-372 & :506-527, membedakan "belum ada data" vs "filter tidak menemukan hasil" + tombol Reset Filter).
- **Validasi form ber-`@error` lengkap:** admin/users/create & edit (12 blok masing-masing, dengan pesan kustom Indonesia di UserController), settings (8), carousel (3), login (`$errors->first()`), change-password (daftar semua error), komentar guru (view.blade.php:373-375, detail.blade juga ber-@error).
- **Alur upload dokumen guru (evaluasi.blade):** validasi klien tipe/ukuran file dengan pesan Indonesia jelas (:404-413), progres "N/7 Dokumen", tombol "Lanjut" dinonaktifkan sampai 7/7 dengan pesan peringatan bila dipaksa (:713-723), auto-scroll ke dokumen pertama yang belum lengkap. (Catatan: fungsi `showToast` lokalnya ikut tertimpa versi layout, tetapi semua pemanggilnya memakai tipe string yang valid sehingga tidak berdampak — kontras dengan proses.blade.)
- **Alur proses guru:** tombol Submit dinonaktifkan + berubah warna sampai semua field valid (validateForm), penghitung karakter per refleksi, auto-save 30 detik diinformasikan di kotak "Perhatian!", modal konfirmasi sebelum submit, modal sukses dengan arah lanjut yang jelas.
- **Konfirmasi aksi destruktif konsisten** via `showConfirmModal` global: hapus supervisi (guru), hapus dokumen, hapus user, reset password, tandai selesai (kepala, dengan penjelasan akibat), minta revisi.
- **Tombol aksi utama jelas per status** di kartu guru: Draft → "Lanjutkan"+"Hapus", Revision → "Revisi" (oranye), lainnya → "Detail"; kepala: "Detail" per kartu; banner catatan revisi tampil untuk guru (my-supervisi.blade.php:174-186).
- **Pesan flash controller seluruhnya berbahasa Indonesia** dan bernada konsisten ("… berhasil …!"): ChangePassword, Settings, UserController, SupervisiController (admin/guru), EvaluasiController, CarouselController; event Livewire `status-updated` punya listener toast di admin/users/index.blade.php:81.
- **Escaping aman untuk toast flash:** `showToast('{{ session('success') }}')` — Blade meng-escape tanda kutip menjadi entitas HTML sehingga string JS tidak pecah.
